namespace EventSystem
{
    /// <summary>
    /// Every event in the project is a member of this enum. Add your own event names here by hand -
    /// this is plain, hand-edited code, there is no generation step.
    /// </summary>
    public enum EventTypes
    {
        ExampleEvent,
    }
}
